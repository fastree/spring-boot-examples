package com.fastree.springboot.netty.nio;

/*
通道的读取，就是将数据从通道读取到缓冲区中；
通道的写入，就是将数据从缓冲区中写入到通道中；

写模式：
    buffer.clear() 或 buffer.compact()
读模式：
    buffer.flip()
 */
public class BufferDemo {
    public static void main(String[] args) {
    }
}
